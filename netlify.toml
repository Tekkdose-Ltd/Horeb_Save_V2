[build]
  command = "npx vite build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html for client-side routing (wouter)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
